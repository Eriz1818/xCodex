# Announcement tips for Codex/xCodex TUI.
# Each [[announcements]] entry is evaluated in order; the last matching one is shown.
# Dates are UTC, formatted as YYYY-MM-DD. `from_date` is inclusive and `to_date` is exclusive.
# `version_regex` matches against the CLI version (env!("CARGO_PKG_VERSION")); omit to apply to all versions.
# `target_app` specifies which app should display the announcement (cli, vsce, ...).

[[announcements]]
content = "Welcome to Codex! Check out the new onboarding flow."
from_date = "2024-10-01"
to_date = "2024-10-15"
target_app = "cli"

[[announcements]]
# Short-lived announcement for the current fork release.
content = "Tip: xcodex supports git worktrees (`/worktree`) and hooks (`/hooks`). See docs/xcodex/ for details."
from_date = "2026-01-10"
to_date = "2026-02-01"
version_regex = "^0\\.1\\.0$"
target_app = "cli"

[[announcements]]
# Short-lived announcement for the current fork release.
content = "Tip: xcodex has themes (`/themes`) and sensitive-path controls. See docs/xcodex/themes.md and docs/xcodex/ignore-files.md."
from_date = "2026-01-23"
to_date = "2026-02-15"
version_regex = "^0\\.3\\.0$"
target_app = "cli"

[[announcements]]
# Short-lived announcement for the current fork release.
content = "Tip: xcodex now supports syntax highlighting and lazy MCP loading. See docs/xcodex/themes.md and docs/xcodex/lazy-mcp-loading.md."
from_date = "2026-02-02"
to_date = "2026-02-12"
version_regex = "^0\\.3\\.5$"
target_app = "cli"

[[announcements]]
# Example entry that should never match (kept as a reference).
content = "This is a test announcement"
version_regex = "^0\\.0\\.0$"
to_date = "2026-05-10"

[[announcements]]
content = "**BREAKING NEWS**: `gpt-5.3-codex` is out! Upgrade to `0.98.0` to try it."
from_date = "2026-02-01"
to_date = "2026-02-15"
version_regex = "^0\\.(?:[0-9]|[1-8][0-9]|9[0-7])\\."
